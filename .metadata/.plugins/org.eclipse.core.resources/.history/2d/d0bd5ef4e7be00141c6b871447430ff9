
public class Iteration implements RegEx{
	RegEx base;
	public Iteration(RegEx base){
		this.base = base;
	}
	
	public int length(String s){
		for(int i=0;i<s.length();i++){
			if(base.matches(s.substring(0,i))){
				return i;
			}
		}
		return -1;
	}
	@Override
	public boolean matches(String s) {
		int len = length(s);
		int jump = len;
		int i =0;
		boolean flag;
		while(i<s.length()){
			if(jump>s.length()){
				break;
			}
			System.out.println(s.substring(i,jump));
			if(!base.matches(s.substring(i,jump))){
				flag = false;
				return flag;
			}
			i = jump;
			jump += len;
		}
		return true;
	}
	public String toString(){
		return "" + base.toString();
	}


}
