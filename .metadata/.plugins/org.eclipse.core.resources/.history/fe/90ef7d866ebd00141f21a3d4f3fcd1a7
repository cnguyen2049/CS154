
public class Concat implements RegEx{
	RegEx first;
	RegEx second;
	public Concat (RegEx first, RegEx second){
		this.first = first;
		this.second = second;
	}
	
	public int findIndex(RegEx reg,String s){
		
		int index = s.lastIndexOf(first.toString());
		return 0;
	}
	@Override
	public boolean matches(String s) {
		int index = s.lastIndexOf(first.toString());
		System.out.println(index);
		int index2 = s.lastIndexOf(second.toString());
		String str1 = s.substring(0,index+1);
		//System.out.println(str1);
		String str2 = s.substring(index2+1,s.length()-1);
		//System.out.println(str2);
		if(first.matches(str1) && second.matches(str2)){
			return true;
		}
		return false;
	}

}
