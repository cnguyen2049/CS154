
public class Option implements RegEx{
	private RegEx base;

	@Override
	public boolean matches(String s) {
		if (s.equals("?")){
			return true;
		}
		return s.isEmpty() || base.matches(s);
	}

	public RegEx getBase() {
		return base;
	}

	public void setBase(RegEx base) {
		this.base = base;
	}

}
